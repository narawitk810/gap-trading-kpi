# PRD.md — KPI การทำงาน GAP TRADING

## ภาพรวมระบบ
ระบบบันทึก KPI การทำงานรายวันของพนักงาน แทนการกรอก Excel
- **ฝั่งพนักงาน**: กรอกข้อมูลผ่านมือถือ ไม่ต้องล็อกอิน
- **ฝั่ง HR/CEO (Admin)**: ดูข้อมูลผ่านลิงก์ที่ส่งใน LINE แล้วเปิดเว็บดูตาราง

---

## หน้าที่ 1: หน้ากรอกข้อมูล (พนักงาน) — `/`

ฟอร์มเดียว กรอกครบในหน้าเดียว ลำดับฟิลด์ดังนี้:

1. **เลือกแผนก** (dropdown/ปุ่มเลือก)
   - ไลฟ์สด, การตลาด, Creative, บัญชี, ธุรการ, บุคคล, Stock, แพค
2. **เลือกวันที่ + เวลาที่กรอก** (date picker, เวลา default = เวลาปัจจุบัน)
3. **ชื่อเล่น** (text input)
4. **ชื่อช่องที่ดูแล** (text input)
5. **สิ่งที่ทำวันนี้** (รายการ, เพิ่มได้สูงสุด 10 ช่อง, ปุ่ม "+ เพิ่มรายการ")
6. **อุปสรรคที่เจอวันนี้** (textarea, ไม่บังคับกรอกก็ได้)
7. **ปุ่มยืนยัน** → แสดง popup สรุปข้อมูลก่อนส่งจริง → กดยืนยันอีกครั้ง → บันทึก
8. หลังบันทึกสำเร็จ: แสดงข้อความ "✅ บันทึกข้อมูลสำเร็จ" + เลขอ้างอิง (เช่น #KPI-xxxx)

**Validation:**
- แผนก, วันที่, ชื่อเล่น, ชื่อช่องที่ดูแล ต้องกรอกก่อนยืนยันได้
- ต้องมี "สิ่งที่ทำวันนี้" อย่างน้อย 1 ช่อง

---

## หน้าที่ 2: หน้า Admin — `/admin`

1. **ตารางข้อมูลทั้งหมด** แสดงคอลัมน์: วันที่ | เวลา | แผนก | ชื่อเล่น | ช่องที่ดูแล | สิ่งที่ทำ (ย่อ) | อุปสรรค
   - กดแถวเพื่อดูรายละเอียดเต็ม (modal/expand)
2. **ตัวกรอง**: เลือกแผนก, เลือกช่วงวันที่, ค้นหาด้วยชื่อเล่น
3. **สรุปรายการของวันนี้** ด้านบนตาราง: จำนวนพนักงานที่กรอกแล้ววันนี้ / แยกตามแผนก
4. **ปุ่ม Export** (เผื่ออนาคต — ไม่บังคับ MVP แรก)

**การเข้าถึง:** ใช้ URL เฉพาะที่ไม่เดาง่าย เช่น `/admin?key=xxxx` (ป้องกันเบื้องต้น)

---

## Data Model

```
KPIEntry {
  id: string (เช่น KPI-0001)
  department: enum [ไลฟ์สด, การตลาด, Creative, บัญชี, ธุรการ, บุคคล, Stock, แพค]
  date: date
  time: time
  nickname: string
  channel_name: string
  tasks: string[] (สูงสุด 10 รายการ)
  obstacles: string
  created_at: datetime
}
```

## Flow สรุป
พนักงานกรอกฟอร์ม → ยืนยัน → บันทึกลงระบบ → HR/CEO เปิดลิงก์ใน LINE → ดูตาราง/สรุปที่ `/admin`
